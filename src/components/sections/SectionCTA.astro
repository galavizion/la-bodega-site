---
type CTA = { label: string; href: string };

const {
  title,
  subtitle,
  bgColor,
  textAlign = "left",
  primaryCta,
  secondaryCta,
  settings,
} = Astro.props as {
  title?: string;
  subtitle?: string;
  bgColor?: string;
  textAlign?: "left" | "center" | "right";
  primaryCta?: CTA | null;
  secondaryCta?: CTA | null;
  settings?: { sectionId?: string };
};

const sectionId = settings?.sectionId;

const resolvedTitle = title ?? "¿Listo para hacerlo bien?";
const resolvedSubtitle = subtitle ?? "Dime qué estás construyendo y te digo cómo lo haría para que convierta.";
const resolvedPrimary = primaryCta ?? { label: "Contáctame", href: "/contacto" };

const bg = bgColor && bgColor !== "none" ? bgColor : null;
const bgTheme = bg === "#000000" ? "section--on-dark"
  : (bg === "#ffffff" || bg?.toLowerCase() === "#FFD700") ? "section--on-light"
  : "";
---

<section id={sectionId} class={`section ${bgTheme}`} style={bg ? `background-color:${bg}` : undefined}>
  <div class="container">
    <div class="cta__card">
      <div class="cta__copy" style={`text-align:${textAlign}`}>
        <h2>{resolvedTitle}</h2>
        <p>{resolvedSubtitle}</p>
      </div>
      <div class="cta__actions">
        <a class="btn btn--primary" href={resolvedPrimary.href}>{resolvedPrimary.label}</a>
        {secondaryCta?.href ? (
          <a class="btn btn--ghost" href={secondaryCta.href}>{secondaryCta.label}</a>
        ) : null}
      </div>
    </div>
  </div>
</section>

<style>
  .container{ max-width: 1100px; margin: 0 auto; padding: 0 20px; }
  .cta__card{
    border-radius: 18px;
    padding: 28px;
    border: 1px solid rgba(0,0,0,.12);
    display:flex;
    gap: 18px;
    align-items:center;
    justify-content: space-between;
    background: linear-gradient(180deg, rgba(230, 6, 6, 0.918), rgba(139, 11, 11, 0.938));
  }
  .cta__copy h2{ margin: 0 0 6px; color: #fff; }
  .cta__copy p{ margin: 0; opacity: .85; line-height: 1.6; max-width: 60ch; color: #fff; }
  .cta__actions{ display:flex; gap: 10px; flex-wrap: wrap; }
  .btn{ display:inline-flex; align-items:center; justify-content:center; padding: 10px 14px; border-radius: 12px; text-decoration:none; font-weight: 600; }
  .btn--ghost{ border:1px solid rgba(255,255,255,.3); color:inherit; background:transparent; }
</style>